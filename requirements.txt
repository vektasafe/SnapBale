# SnapBale Dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Web server
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0

# Image processing
rembg>=2.0.50
Pillow>=10.0.0
opencv-python>=4.8.0

# Database
# sqlite3 is built into Python — no install needed

# Utilities
pathlib2>=2.3.7

# Raspberry Pi hardware (only install on Pi)
# Uncomment when deploying to Pi:
# RPi.GPIO>=0.7.1
# rpi-ws281x>=5.0.0
# picamera2>=0.3.12
# pigpio>=1.78
